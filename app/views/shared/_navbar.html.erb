<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <a class="navbar-brand font-weight-bold" href="..">MyTwitter</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <% if user_signed_in? %>
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <%= link_to current_user.username, edit_user_registration_path, class: "btn btn-light btn-sm" %>
            </li>
            <li class="nav-item">            
                <%= link_to "Logout", destroy_user_session_path, method: :delete ,class:"btn btn-light btn-sm ml-2" %>
            </li>
        </ul>
    <% else %>
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <%= link_to 'Log In', new_user_session_path, class: "btn btn-light" %>
            </li>
            <li class="nav-item">
                <%= link_to 'Sign Up', new_user_registration_path, class: "btn btn-light ml-2" %>
            </li>
        </ul>
    <% end %>
    </ul>
  </div>
</nav>
